<!--
TEMPLATE -- generated by the `bionemo-recipes-acceleration` skill. Write to the TARGET repo root.

There are two variants below. Use exactly one:
  A. PORT COMPLETED  -- delete the hard-stop section.
  B. HARD STOP       -- delete the port section. No files in the target were modified.

Delete this comment and every unused placeholder before writing the final file.
-->

# BioNeMo acceleration report

**Target:** `{{TARGET_REPO}}`
**Branch:** `{{BRANCH}}`
**Date:** {{DATE}}
**Outcome:** {{PORTED | HARD STOP -- no files modified}}

______________________________________________________________________

## Variant B — hard stop

*(Delete this whole section if the port completed.)*

### What was detected

{{DETECTED_ARCHITECTURE}} — identified from `{{EVIDENCE_FILE}}::{{EVIDENCE_SYMBOL}}`.

### Closest BioNeMo reference

`{{CLOSEST_FAMILY}}` (reference: `{{REFERENCE_PATH}}`)

| Rubric axis | Target | Reference | Verdict |
| --- | --- | --- | --- |
| Attention pattern | {{...}} | {{...}} | {{match/mismatch}} |
| Normalization | {{...}} | {{...}} | {{match/mismatch}} |
| MLP form | {{...}} | {{...}} | {{match/mismatch}} |
| Positional encoding | {{...}} | {{...}} | {{match/mismatch}} |
| Attention grouping | {{...}} | {{...}} | {{match/mismatch}} |
| Head structure | {{...}} | {{...}} | {{match/mismatch}} |

### Why this is disqualifying

{{DISQUALIFYING_REASON}}. This must be one of: an architecture with no TE analogue (diffusion,
GNN/equivariant, state-space); an attention-pattern mismatch, which is not recoverable by
configuration; a model definition that cannot be located; or the absence of a runnable forward pass,
which leaves no way to validate a port at all.

A mismatch on normalization, MLP form, positional encoding, attention grouping, or head structure is
**not** a valid reason to stop — those select the reference and the port depth. If one of those is
the only problem, this is the wrong report variant; port at the depth the block supports instead.

**No files were modified.**

### What would still be safe, by hand

{{SAFE_ACCELERATIONS}} — for example TE `FusedAdam` and `torch.compile` are architecture-agnostic.

> These are **not applied and not validated** by this skill.

### If you want to port manually

Start from `{{NEAREST_RECIPE}}`.

______________________________________________________________________

## Variant A — port completed

*(Delete this whole section if the skill hard-stopped.)*

### 1. Architecture match

| | |
| --- | --- |
| Family | {{FAMILY}} |
| Reference implementation | `{{REFERENCE_PATH}}` |
| Confidence | {{CONFIDENCE}} |
| Deciding evidence | `{{EVIDENCE_FILE}}::{{EVIDENCE_SYMBOL}}` |

Rubric: attention `{{...}}`, normalization `{{...}}`, MLP `{{...}}`, positional `{{...}}`,
grouping `{{...}}`, head `{{...}}`.

### 2. Hardware and Transformer Engine

| | |
| --- | --- |
| Device | {{DEVICE_NAME}} (compute capability {{COMPUTE_CAPABILITY}}) |
| Devices visible | {{DEVICE_COUNT}} |
| torch | {{TORCH_VERSION}} |
| Transformer Engine installed | {{TE_VERSION}} |
| Recommended | `nvcr.io/nvidia/pytorch:26.04-py3`, or `transformer-engine[pytorch]==2.9.0` + `torch==2.9.0` for a fresh venv |

Recipe support, as reported by Transformer Engine's own `check_*_support()`:

| Recipe | Supported | Reason |
| --- | --- | --- |
| DelayedScaling | {{...}} | {{...}} |
| Float8CurrentScaling | {{...}} | {{...}} |
| Float8BlockScaling | {{...}} | {{...}} |
| MXFP8BlockScaling | {{...}} | {{...}} |
| NVFP4BlockScaling | {{...}} | {{...}} |

Caveats: {{HARDWARE_CAVEATS}}

**The TE version was not changed.** A suggested dependency patch is in `.bionemo-accel/`.

### 3. Precision selection

Measured with Transformer Engine's `benchmarks/gemm/benchmark_gemm.py` in {{model config | manual shape}} mode.

| Recipe | Autocast speedup vs BF16 | Pre-quantized speedup |
| --- | --- | --- |
| {{...}} | {{...}}× | {{...}}× |

**Recommendation: `{{RECOMMENDED_RECIPE}}`** — runner-up `{{RUNNER_UP}}`, margin {{MARGIN}}.

> **These are GEMM numbers, not training numbers.** Precision affects only the linear projections.
> Attention, LayerNorm/RMSNorm, activations, and AllReduce are precision-agnostic, so the figures
> above are an **upper bound** on end-to-end speedup. The autocast-vs-pre-quantized gap
> ({{QUANT_OVERHEAD}}) is dynamic quantization overhead. Measured end-to-end result is in §6.

{{NVFP4_CAVEAT_IF_APPLICABLE}}

### 4. Changes made

Rewrite depth: **{{DEPTH}}** — {{DEPTH_RATIONALE}}

| File | Change |
| --- | --- |
| `{{...}}` | {{...}} |

Accelerations applied:

- [ ] TE `TransformerLayer` port
- [ ] HF↔TE state-dict converter
- [ ] FP8/FP4 config block (**shipped disabled**)
- [ ] Per-layer precision resolution
- [ ] THD sequence packing
- [ ] `quantized_model_init` + `FusedAdam(master_weights=True)`
- [ ] `PerfLogger` throughput instrumentation

### 5. Not applied, and why

| Acceleration | Why not |
| --- | --- |
| {{...}} | {{...}} |

### 6. Validation

**Tier 1 — parity check** (`parity_check.py`): {{RESULT}}

| Check | Result |
| --- | --- |
| Forward logits/loss parity vs original (bf16) | {{...}} |
| Backward runs, gradients finite | {{...}} |
| Recipe sweep at FP8 tolerances | {{...}} |
| BSHD vs THD equivalence | {{...}} |

Tolerance overrides: {{TOLERANCE_OVERRIDES_AND_REASONS}}

**Tier 2 — `BaseModelTest` harness:** {{RESULT_OR_NOT_APPLICABLE}}

{{PYTEST_SUMMARY}}

**Tier 3 — CI parity:** {{RESULT_OR_NOT_APPLICABLE}}

> **What CI did not prove.** PR CI runs on a single L4. Everything gated on `requires_multi_gpu` or
> `requires_datacenter_hardware` (H100/H200/B100/B200/B300) was **skipped, not passed** — including
> the MXFP8 and NVFP4 paths, which need Blackwell. Tests skipped here: {{SKIPPED_TESTS}}.
> Run these on a DGX/GB200 node before production training.

**Measured throughput** (`PerfLogger`, {{N_STEPS}} steps):

| Configuration | tokens/sec/GPU | Peak memory (GB) |
| --- | --- | --- |
| Baseline (original, BF16) | {{...}} | {{...}} |
| Ported, BF16 | {{...}} | {{...}} |
| Ported, packing | {{...}} | {{...}} |
| Ported, packing + {{RECOMMENDED_RECIPE}} | {{...}} | {{...}} |

### 7. Turning on low precision

Low precision ships **disabled**. To enable the recommendation:

```yaml
fp8_config:
  enabled: true  # <-- this line
  fp8_recipe: transformer_engine.common.recipe.{{RECOMMENDED_RECIPE}}
  fp8_format: "{{RECOMMENDED_FORMAT}}"
  fp8_recipe_kwargs: {}
```

### 8. Recommended next steps

1. {{...}}
