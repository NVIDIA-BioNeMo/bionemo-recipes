diff --git a/pyproject.toml b/pyproject.toml
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -3,7 +3,7 @@ requires = ["setuptools>=42", "wheel>=0.46.2"]
 build-backend = "setuptools.build_meta"

 [tool.setuptools]
-packages = ["nemo_rl"]
+packages = { find = { include = ["nemo_rl*"] } }

 [tool.setuptools.dynamic]
 version = { attr = "nemo_rl.__version__" }                      # any module attribute compatible with ast.literal_eval
@@ -13,6 +13,6 @@ name = "nemo-rl"
 dynamic = ["version", "readme"]
 description = "NeMo RL: A Scalable and Efficient Post-Training Library for Models Ranging from 1 GPU to 1000s, and from Tiny to >100B Parameters"
-requires-python = ">=3.13.13,<3.14"
+requires-python = ">=3.10"
 license = { text = "Apache 2.0" }
 maintainers = [

diff --git a/nemo_rl/__init__.py b/nemo_rl/__init__.py
--- a/nemo_rl/__init__.py
+++ b/nemo_rl/__init__.py
@@ -56,0 +57,4 @@
+
+# Recipe-patch capability marker used by CUDA-free installation checks. The source-level
+# recipe tests separately verify the worker and policy-offload implementation sites.
+EVO2_GRAPH_STORAGE_LIFECYCLE_VERSION = 2
diff --git a/nemo_rl/algorithms/grpo.py b/nemo_rl/algorithms/grpo.py
index e5647bae7..b8603d049 100644
--- a/nemo_rl/algorithms/grpo.py
+++ b/nemo_rl/algorithms/grpo.py
@@ -2088,6 +2088,7 @@ def grpo_train(
             # A central place to store logging data that won't be deleted until the loop ends
             metrics_logging_data = dict()
             metrics = dict()
+            rollout_timing_metrics: dict[str, float] = {}

             if master_config.data["use_multiple_dataloader"]:
                 print(
@@ -2247,6 +2247,7 @@ def grpo_train(
                             policy_generation.get_logger_metrics()
                         )

+                    rollout_metrics, rollout_timing_metrics = split_environment_timing_metrics(rollout_metrics)
                     metrics_logging_data["mean_gen_tokens_per_sample"] = (
                         rollout_metrics["mean_gen_tokens_per_sample"]
                     )
@@ -2837,6 +2838,7 @@ def grpo_train(
             timing_metrics: dict[str, float] = timer.get_timing_metrics(
                 reduction_op="sum"
             )  # type: ignore
+            timing_metrics.update(rollout_timing_metrics)
             # track example with high token mult prob error above 1.05
             if metrics["token_mult_prob_error"] > 1.05:
                 logger.log_plot_token_mult_prob_error(
@@ -2915,9 +2917,7 @@ def grpo_train(
             print(f"  • Total step time: {total_time:.2f}s", flush=True)

             # Display all other timing metrics
-            for k, v in sorted(
-                timing_metrics.items(), key=lambda item: item[1], reverse=True
-            ):
+            for k, v in _iter_display_timing_metrics(timing_metrics):
                 if k != "total_step_time":
                     percent = (v / total_time * 100) if total_time > 0 else 0
                     print(f"  • {k}: {v:.2f}s ({percent:.1f}%)", flush=True)
@@ -2976,6 +2976,32 @@ def grpo_train(
         current_step = 0  # Reset step counter for new epoch


+ENV_TIMING_METRIC_PREFIX = "__timing__/"
+
+
+def split_environment_timing_metrics(metrics: dict[str, Any]) -> tuple[dict[str, Any], dict[str, float]]:
+    """Split environment timing metrics so NeMo-RL logs them through timing/* prefixes."""
+    regular_metrics: dict[str, Any] = {}
+    timing_metrics: dict[str, float] = {}
+    for key, value in metrics.items():
+        if isinstance(key, str) and key.startswith(ENV_TIMING_METRIC_PREFIX):
+            timing_metrics[key[len(ENV_TIMING_METRIC_PREFIX) :]] = float(value)
+        else:
+            regular_metrics[key] = value
+    return regular_metrics, timing_metrics
+
+
+def _iter_display_timing_metrics(timing_metrics: dict[str, float]):
+    """Yield timing metrics suitable for the human-readable console summary."""
+    return (
+        (key, value)
+        for key, value in sorted(
+            timing_metrics.items(), key=lambda item: item[1], reverse=True
+        )
+        if not key.endswith("_unix_s")
+    )
+
+
 def validate(
     policy_generation: GenerationInterface,
     val_dataloader: Optional[StatefulDataLoader],
@@ -3000,6 +3026,8 @@ def validate(
         total_rewards = []
         total_lengths = []
         all_message_logs = []  # Collect all message logs
+        validation_rollout_metrics = []
+        additional_timing_metrics_to_report = {}

         max_batches = (
             master_config.grpo["max_val_samples"]
@@ -3009,7 +3037,6 @@ def validate(
             if batch_idx >= max_batches:
                 break

-            additional_metrics_to_report = dict()
             # Generate responses (updates the LLMMessageLogType in batch_with_msg_logs)
             # Use async rollouts when enabled by config/backend defaults.
             # We cascade NeMo-Gym first since NeMo-Gym also uses async rollouts.
@@ -3028,7 +3055,6 @@ def validate(
                 )
                 val_batch = nemo_gym_rollout_result.final_batch
                 gen_metrics = nemo_gym_rollout_result.rollout_metrics
-                additional_metrics_to_report = gen_metrics
             elif _should_use_async_rollouts(master_config):
                 val_batch, gen_metrics = run_async_multi_turn_rollout(
                     policy_generation,
@@ -3050,6 +3076,7 @@ def validate(
                     greedy=False,
                 )

+            validation_rollout_metrics.append(gen_metrics)
             total_rewards.extend(val_batch["total_reward"].tolist())
             total_lengths.append(gen_metrics["mean_gen_tokens_per_sample"])

@@ -3075,6 +3102,20 @@ def validate(
             sum(total_lengths) / len(total_lengths) if len(total_lengths) > 0 else 0.0
         )

+        if len(validation_rollout_metrics) == 1:
+            additional_metrics_to_report = validation_rollout_metrics[0]
+        else:
+            grouped_validation_metrics: dict[str, list] = {}
+            for batch_metrics in validation_rollout_metrics:
+                for key, value in batch_metrics.items():
+                    grouped_validation_metrics.setdefault(key, []).append(value)
+            additional_metrics_to_report = aggregate_rollout_metrics(
+                grouped_validation_metrics
+            )
+
+        additional_metrics_to_report, additional_timing_metrics_to_report = split_environment_timing_metrics(
+            additional_metrics_to_report
+        )
         val_metrics = {
             "accuracy": accuracy,
             "avg_length": avg_length,
@@ -3098,6 +3139,7 @@ def validate(

     # Get timing metrics
     timing_metrics = timer.get_timing_metrics(reduction_op="sum")
+    timing_metrics.update(additional_timing_metrics_to_report)
     validation_time = timing_metrics.get("total_validation_time", 0)

     # Print summary of validation results
@@ -3485,6 +3527,7 @@ def async_grpo_train(
     # Main training loop
     try:
         while step < master_config.grpo["max_num_steps"]:
+            rollout_timing_metrics: dict[str, float] = {}
             print(
                 f"\n{'=' * 25} Step {step + 1}/{master_config.grpo['max_num_steps']} {'=' * 25}"
             )
@@ -3554,6 +3597,7 @@ def async_grpo_train(
                         for k, v in t["rollout_metrics"].items():
                             per_group_metrics.setdefault(k, []).append(v)
                     rollout_metrics = aggregate_rollout_metrics(per_group_metrics)
+                    rollout_metrics, rollout_timing_metrics = split_environment_timing_metrics(rollout_metrics)

                 # Enforce fixed training batch: num_prompts_per_step * num_generations_per_prompt
                 expected_batch_size = (
@@ -4035,6 +4079,7 @@ def async_grpo_train(
             timing_metrics: dict[str, float] = timer.get_timing_metrics(
                 reduction_op="sum"
             )
+            timing_metrics.update(rollout_timing_metrics)

             # Add buffer stats
             buffer_size_current = ray.get(replay_buffer.size.remote())
@@ -4082,9 +4127,7 @@ def async_grpo_train(
             print("\n⏱️  Timing:")
             total_time = timing_metrics.get("total_step_time", 0)
             print(f"  • Total step time: {total_time:.2f}s")
-            for k, v in sorted(
-                timing_metrics.items(), key=lambda item: item[1], reverse=True
-            ):
+            for k, v in _iter_display_timing_metrics(timing_metrics):
                 if k != "total_step_time":
                     percent = (v / total_time * 100) if total_time > 0 else 0
                     print(f"  • {k}: {v:.2f}s ({percent:.1f}%)")
diff --git a/nemo_rl/experience/rollouts.py b/nemo_rl/experience/rollouts.py
index 44a11cddd..c12ec4e1d 100644
--- a/nemo_rl/experience/rollouts.py
+++ b/nemo_rl/experience/rollouts.py
@@ -571,6 +571,39 @@ def calculate_rewards(
     )


+def collect_environment_metrics(
+    batch: BatchedDataDict[DatumSpec],
+    task_to_env: dict[str, EnvironmentInterface],
+) -> dict[str, Any]:
+    """Collect optional scalar rollout metrics from environments."""
+    task_names = batch["task_name"]
+    futures = []
+    future_task_names = []
+    for task_name, env in task_to_env.items():
+        metric_method = getattr(env, "global_post_process_and_metrics", None)
+        if metric_method is None:
+            continue
+        indices = [
+            i for i, batch_task_name in enumerate(task_names) if batch_task_name == task_name
+        ]
+        if not indices:
+            continue
+        selected_batch = batch.select_indices(torch.tensor(indices, dtype=torch.long))
+        futures.append(metric_method.remote(selected_batch))  # type: ignore[attr-defined]
+        future_task_names.append(task_name)
+
+    metrics: dict[str, Any] = {}
+    if not futures:
+        return metrics
+    results = list(zip(future_task_names, ray.get(futures), strict=False))
+    for task_name, result in results:
+        _, env_metrics = result
+        for key, value in env_metrics.items():
+            metric_key = key if key.startswith("__timing__/") else f"{task_name}/{key}"
+            metrics[metric_key] = value
+    return metrics
+
+
 def run_multi_turn_rollout(
     policy_generation: GenerationInterface,
     input_batch: BatchedDataDict[DatumSpec],
@@ -717,6 +748,8 @@ def run_multi_turn_rollout(
         truncation_mask = torch.zeros_like(env_output.terminateds, dtype=torch.bool)
         for i, global_idx in enumerate(active_indices.tolist()):
             env_obs_content = env_output.observations[i]["content"]
+            if env_output.metadata[i] is not None:
+                current_batch["extra_env_info"][global_idx] = env_output.metadata[i]
             # Tokenize the raw content from the environment
             # TODO @sahilj: handle if we want these subsequent messages to have a chat template
             tokenized_obs = tokenizer(
@@ -823,6 +856,7 @@ def run_multi_turn_rollout(
             sample_env_token_counts.float().mean().item()
         ),
     }
+    rollout_metrics.update(collect_environment_metrics(current_batch, task_to_env))
     return current_batch, rollout_metrics


@@ -1289,6 +1323,7 @@ def run_async_multi_turn_rollout(
         rollout_metrics["histogram/total_tokens_length"] = [
             t for m in all_sample_metrics for t in m["turn_total_tokens"]
         ]
+        rollout_metrics.update(collect_environment_metrics(final_batch, task_to_env))

         return final_batch, rollout_metrics

diff --git a/nemo_rl/models/generation/megatron/megatron_generation.py b/nemo_rl/models/generation/megatron/megatron_generation.py
index b8c2a4f2b..b4a218eb5 100644
--- a/nemo_rl/models/generation/megatron/megatron_generation.py
+++ b/nemo_rl/models/generation/megatron/megatron_generation.py
@@ -12,6 +12,7 @@
 # See the License for the specific language governing permissions and
 # limitations under the License.

+import importlib
 from typing import TYPE_CHECKING, Any, AsyncGenerator, Optional

 import ray
@@ -32,6 +33,34 @@ if TYPE_CHECKING:
     from nemo_rl.models.policy.lm_policy import Policy


+def _load_generation_adapter(generation_config: dict[str, Any]) -> Any | None:
+    """Load an optional generation adapter from ``mcore_generation_config``."""
+    mcore_generation_config = generation_config.get("mcore_generation_config", {}) or {}
+    adapter_path = mcore_generation_config.get("generation_adapter")
+    if not adapter_path:
+        return None
+
+    try:
+        module_name, class_name = adapter_path.split(":", 1)
+    except ValueError as exc:
+        raise ValueError(
+            "generation_adapter must use 'module:ClassName' import-path syntax"
+        ) from exc
+
+    module = importlib.import_module(module_name)
+    adapter_cls = getattr(module, class_name)
+    adapter_config = mcore_generation_config.get("generation_adapter_config", {}) or {}
+    return adapter_cls(adapter_config)
+
+
+def _adapter_requires_all_workers(adapter: Any, generation_config: dict[str, Any]) -> bool:
+    """Return whether an adapter must run on all model-parallel workers."""
+    requires_all_workers = getattr(adapter, "requires_all_workers", False)
+    if callable(requires_all_workers):
+        return bool(requires_all_workers(generation_config))
+    return bool(requires_all_workers)
+
+
 class MegatronGeneration(GenerationInterface):
     """Generation interface backed by Megatron (colocated or non-colocated)."""

@@ -91,6 +120,7 @@ class MegatronGeneration(GenerationInterface):
         # `self._policy_config` keeps a reference to the full PolicyConfig.
         self._policy_config = config
         self.cfg: MCoreGenerationConfig = config["generation"]
+        self._generation_adapter = _load_generation_adapter(self.cfg)
         # Populated after the first prepare_for_generation (which starts the HTTP server).
         self.dp_openai_server_base_urls: list[Optional[str]] = []

@@ -167,9 +196,9 @@ class MegatronGeneration(GenerationInterface):
     ) -> BatchedDataDict[GenerationOutputSpec]:
         """Generate a batch of data using the Megatron generation backend.

-        mcore's data-parallel coordinator only accepts requests from DP rank 0 —
-        the other workers' engine loops drain the coordinator queue but never
-        receive a Python-side call. So we dispatch straight to worker 0.
+        Adapters that require all model-parallel workers receive one input shard
+        per DP replica. The standard MCore coordinator fallback still dispatches
+        only to worker 0 because it accepts Python requests from DP rank 0.

         Args:
             data: BatchedDataDict containing input_ids and input_lengths.
@@ -178,6 +207,66 @@ class MegatronGeneration(GenerationInterface):
         Returns:
             BatchedDataDict conforming to GenerationOutputSpec.
         """
+        adapter = self._generation_adapter
+        if adapter is not None and _adapter_requires_all_workers(adapter, self.cfg):
+            worker_group = self._policy.worker_group
+            dp_size = worker_group.sharding_annotations.get_axis_size(
+                "data_parallel"
+            )
+            if data.size < dp_size:
+                raise ValueError(
+                    f"Evo2 generation batch size {data.size} must be at least "
+                    f"data-parallel size {dp_size}"
+                )
+            base_shard_size, larger_shard_count = divmod(data.size, dp_size)
+            sharded_data = []
+            shard_start = 0
+            for dp_rank in range(dp_size):
+                shard_size = base_shard_size + int(dp_rank < larger_shard_count)
+                shard_end = shard_start + shard_size
+                sharded_data.append(
+                    data.select_indices(list(range(shard_start, shard_end)))
+                )
+                shard_start = shard_end
+            replicated_axes = [
+                "context_parallel",
+                "tensor_parallel",
+                "pipeline_parallel",
+            ]
+            future_bundle = worker_group.run_all_workers_sharded_data(
+                "generate_with_adapter",
+                data=sharded_data,
+                in_sharded_axes=["data_parallel"],
+                replicate_on_axes=replicated_axes,
+                output_is_replicated=replicated_axes,
+                common_kwargs={"greedy": greedy},
+            )
+            results = worker_group.get_all_worker_results(future_bundle)
+            if len(results) != dp_size:
+                raise RuntimeError(
+                    f"Evo2 generation expected {dp_size} data-parallel results, "
+                    f"received {len(results)}"
+                )
+            for dp_rank, (input_shard, result) in enumerate(
+                zip(sharded_data, results, strict=True)
+            ):
+                if result is None or result.size != input_shard.size:
+                    result_size = None if result is None else result.size
+                    raise RuntimeError(
+                        f"Evo2 generation DP rank {dp_rank} returned {result_size} rows "
+                        f"for an input shard of {input_shard.size} rows"
+                    )
+            combined = BatchedDataDict.from_batches(
+                results,
+                pad_value_dict={"output_ids": self.cfg["_pad_token_id"]},
+            )
+            if combined.size != data.size:
+                raise RuntimeError(
+                    f"Evo2 generation gathered {combined.size} rows for a global "
+                    f"input batch of {data.size} rows"
+                )
+            return combined
+
         future = self._policy.worker_group.run_single_worker_single_data(
             method_name="generate",
             worker_idx=0,
diff --git a/nemo_rl/models/generation/megatron/megatron_worker.py b/nemo_rl/models/generation/megatron/megatron_worker.py
index 0a0721dd9..00eebb24a 100644
--- a/nemo_rl/models/generation/megatron/megatron_worker.py
+++ b/nemo_rl/models/generation/megatron/megatron_worker.py
@@ -14,10 +14,12 @@

 import asyncio
 import gc
+import importlib
 import os
 import threading
 import time
 import warnings
+from types import MethodType
 from typing import AsyncGenerator, Optional

 import requests
@@ -49,1 +51,10 @@
+def _round_up_request_capacity(request_count: int, tensor_parallel_size: int) -> int:
+    """Round a positive inference request capacity up to a TP multiple."""
+    if request_count < 1 or tensor_parallel_size < 1:
+        raise ValueError("request_count and tensor_parallel_size must both be positive")
+    return (
+        (request_count + tensor_parallel_size - 1) // tensor_parallel_size
+    ) * tensor_parallel_size
+
+
 class MegatronGenerationMixin:
@@ -73,13 +84,70 @@ class MegatronGenerationMixin:
         self._inference_loop = None
         self._inference_thread = None

+    def _load_generation_adapter(self):
+        """Load an optional generation adapter configured by import path."""
+        mcore_generation_config = self.cfg["generation"].get(
+            "mcore_generation_config", {}
+        ) or {}
+        adapter_path = mcore_generation_config.get("generation_adapter")
+        if not adapter_path:
+            return None
+
+        cached_adapter = getattr(self, "_generation_adapter", None)
+        cached_path = getattr(self, "_generation_adapter_path", None)
+        cached_config = getattr(self, "_generation_adapter_config", None)
+        adapter_config = mcore_generation_config.get("generation_adapter_config", {}) or {}
+        if (
+            cached_adapter is not None
+            and cached_path == adapter_path
+            and cached_config == adapter_config
+        ):
+            return cached_adapter
+
+        try:
+            module_name, class_name = adapter_path.split(":", 1)
+        except ValueError as exc:
+            raise ValueError(
+                "generation_adapter must use 'module:ClassName' import-path syntax"
+            ) from exc
+
+        module = importlib.import_module(module_name)
+        adapter_cls = getattr(module, class_name)
+        adapter = adapter_cls(adapter_config)
+        self._generation_adapter = adapter
+        self._generation_adapter_path = adapter_path
+        self._generation_adapter_config = dict(adapter_config)
+        return adapter
+
+    def _generation_adapter_requires_persistent_model_storage(self) -> bool:
+        """Return whether graph-captured model allocations must remain resident."""
+        adapter = self._load_generation_adapter()
+        requirement = getattr(adapter, "requires_persistent_model_storage", False)
+        if callable(requirement):
+            return bool(requirement(self))
+        return bool(requirement)
+
+    def _generation_adapter_model_refit_complete(self) -> None:
+        """Notify the adapter after colocated policy weights have changed."""
+        adapter = self._load_generation_adapter()
+        callback = getattr(adapter, "model_refit_complete", None)
+        if callable(callback):
+            callback(self)
+
+    def _generation_adapter_preserves_optimizer_state(self) -> bool:
+        """Return whether an adapter has opted to retain optimizer state during generation."""
+        adapter = self._load_generation_adapter()
+        preserve = getattr(adapter, "preserve_optimizer_state_during_generation", False)
+        if callable(preserve):
+            return bool(preserve(self))
+        return bool(preserve)
+
     def _initialize_inference_engine(self, mcore_generation_config: dict) -> None:
         """Initialize the persistent inference engine and client."""
         # TODO: Switch to standardized Megatron API.
         if self._inference_engine_initialized:
             return

-        from megatron.core.inference.config import MambaInferenceStateConfig
         from megatron.core.inference.contexts.dynamic_context import (
             DynamicInferenceContext,
         )
@@ -113,9 +158,44 @@ class MegatronGenerationMixin:
             "materialize_only_last_token_logits"
         ]
         num_speculative_tokens = mcore_generation_config["num_speculative_tokens"]
+        evo2_batched_decode_size = int(
+            mcore_generation_config.get("prompt_batch_size")
+            or mcore_generation_config.get("evo2_batched_decode_size")
+            or 1
+        )
         max_requests = mcore_generation_config.get("max_requests")
+        if max_requests is not None or evo2_batched_decode_size > 1:
+            tensor_parallel_size = int(
+                self.cfg["megatron_cfg"]["tensor_model_parallel_size"]
+            )
+            max_requests = _round_up_request_capacity(
+                max(int(max_requests or 0), evo2_batched_decode_size),
+                tensor_parallel_size,
+            )
+
+        evo2_bind_hyena_packed_views = None
+        evo2_bind_hyena_packed_views_batch = None
+        try:
+            from bionemo.evo2.models.evo2_provider import (
+                bind_hyena_packed_views_to_dynamic_context_batch,
+                bind_hyena_packed_views_to_dynamic_context,
+                build_evo2_mamba_inference_state_config,
+            )
+        except ModuleNotFoundError:
+            bind_hyena_packed_views_to_dynamic_context = None
+            bind_hyena_packed_views_to_dynamic_context_batch = None
+            build_evo2_mamba_inference_state_config = None
+
+        raw_model = unwrap_model(self.model)
+        has_evo2_hyena_state = hasattr(getattr(raw_model, "decoder", raw_model), "hyena_state_shapes_per_request")
+        if has_evo2_hyena_state and build_evo2_mamba_inference_state_config is not None:
+            mamba_inference_state_config = build_evo2_mamba_inference_state_config(raw_model)
+            evo2_bind_hyena_packed_views = bind_hyena_packed_views_to_dynamic_context
+            evo2_bind_hyena_packed_views_batch = bind_hyena_packed_views_to_dynamic_context_batch
+        else:
+            from megatron.core.inference.config import MambaInferenceStateConfig

-        mamba_inference_state_config = MambaInferenceStateConfig.from_model(self.model)
+            mamba_inference_state_config = MambaInferenceStateConfig.from_model(self.model)
         is_hybrid_model = mamba_inference_state_config is not None
         if is_hybrid_model:
             if (
@@ -182,6 +262,28 @@ class MegatronGenerationMixin:
         self.inference_context = DynamicInferenceContext(
             self.model.config, inference_config
         )
+        if evo2_bind_hyena_packed_views is not None:
+            hyena_model = raw_model
+            original_add_request = self.inference_context.add_request
+            self.inference_context.evo2_max_batched_decode_requests = evo2_batched_decode_size
+
+            def evo2_add_request(context, req, prefill_chunk_length=None):
+                request_slot = int(context.total_request_count)
+                result = original_add_request(req, prefill_chunk_length=prefill_chunk_length)
+                active_request_count = int(context.total_request_count) - int(context.paused_request_count)
+                if evo2_batched_decode_size > 1 and active_request_count > 1:
+                    active_slice = slice(int(context.paused_request_count), int(context.total_request_count))
+                    mamba_slots = context.mamba_metadata.request_to_mamba_state_idx[active_slice]
+                    evo2_bind_hyena_packed_views_batch(hyena_model, context, request_slots=mamba_slots)
+                    context.evo2_batched_decode_enabled = active_request_count <= evo2_batched_decode_size
+                else:
+                    mamba_slot = int(context.mamba_metadata.request_to_mamba_state_idx[request_slot].item())
+                    evo2_bind_hyena_packed_views(hyena_model, context, request_slot=mamba_slot)
+                    context.evo2_batched_decode_enabled = False
+                return result
+
+            self.inference_context.add_request = MethodType(evo2_add_request, self.inference_context)
+            self.inference_context.materialize_only_last_token_logits = materialize_only_last_token_logits
         self.inference_wrapped_model = GPTInferenceWrapper(
             self.model, self.inference_context
         )
@@ -347,9 +449,17 @@ class MegatronGenerationMixin:
         log_gpu_memory("finish_generation START")

         lang_module = unwrap_model(self.model)
+        adapter = self._load_generation_adapter()
+        bypasses_persistent_engine = adapter is not None and getattr(
+            adapter, "bypasses_persistent_mcore_engine", False
+        )

         if self.is_generation_colocated:
-            if self._inference_engine_initialized and not self._inference_engine_asleep:
+            if (
+                not bypasses_persistent_engine
+                and self._inference_engine_initialized
+                and not self._inference_engine_asleep
+            ):
                 self._sleep()
             cuda_graph_impl = self.cfg["generation"]["mcore_generation_config"][
                 "cuda_graph_impl"
@@ -367,6 +477,14 @@ class MegatronGenerationMixin:
             gc.collect()
             torch.cuda.empty_cache()

+        finish_worker = (
+            getattr(adapter, "finish_worker", None) if adapter is not None else None
+        )
+        if callable(finish_worker):
+            finish_worker(self)
+            gc.collect()
+            torch.cuda.empty_cache()
+
         log_gpu_memory("finish_generation END")

     def prepare_for_generation(self, tags=None, **kwargs) -> None:
@@ -399,6 +517,14 @@ class MegatronGenerationMixin:
         if cuda_graph_impl != "none":
             toggle_cuda_graphs(lang_module, set_to=cuda_graph_impl)

+        adapter = self._load_generation_adapter()
+        if adapter is not None and getattr(adapter, "bypasses_persistent_mcore_engine", False):
+            # The adapter drives this same colocated model directly and owns its pointer-stable
+            # dynamic context. The graph toggle above still switches inference-only module state;
+            # only construction/wakeup of MCore's separate coordinator engine is bypassed.
+            log_gpu_memory("prepare_for_generation END")
+            return
+
         # tags=["weights"] means we are inside refit_policy_generation between
         # suspend_for_refit and the weight transfer — the engine was intentionally
         # paused and waking it now would race NVSHMEM init / weight transfer against
@@ -428,6 +539,13 @@ class MegatronGenerationMixin:
         top_p_val = (
             0.0 if greedy else (float(top_p_cfg) if top_p_cfg is not None else 0.0)
         )
+        mcore_generation_config = self.cfg["generation"].get("mcore_generation_config", {})
+        evo2_batched_decode_size = int(
+            mcore_generation_config.get("prompt_batch_size")
+            or mcore_generation_config.get("evo2_batched_decode_size")
+            or 1
+        )
+        termination_id = -1 if evo2_batched_decode_size > 1 else self.megatron_tokenizer.eod

         return SamplingParams(
             temperature=self.cfg["generation"]["temperature"] if not greedy else 0,
@@ -436,7 +554,7 @@ class MegatronGenerationMixin:
             skip_prompt_log_probs=True,
             return_log_probs=True,
             num_tokens_to_generate=self.cfg["generation"]["max_new_tokens"],
-            termination_id=self.megatron_tokenizer.eod,
+            termination_id=termination_id,
             stop_words=stop_words,
         )

@@ -580,6 +698,19 @@ class MegatronGenerationMixin:

         return self._parse_result_to_batched_data_dict(data, result)

+    @wrap_with_nvtx_name("megatron_policy_worker/generate_with_adapter")
+    def generate_with_adapter(
+        self, *, data: BatchedDataDict[GenerationDatumSpec], greedy: bool = False
+    ) -> BatchedDataDict[GenerationOutputSpec] | None:
+        """Delegate generation to a configured adapter when present."""
+        adapter = self._load_generation_adapter()
+        if adapter is None:
+            return None
+        generate_worker = getattr(adapter, "generate_worker", None)
+        if callable(generate_worker):
+            return generate_worker(self, data=data, greedy=greedy)
+        return adapter.generate(self, data=data, greedy=greedy)
+
     async def generate_async(
         self, data: BatchedDataDict[GenerationDatumSpec], greedy: bool = False
     ) -> AsyncGenerator[tuple[int, BatchedDataDict[GenerationOutputSpec]], None]:
diff --git a/nemo_rl/models/megatron/setup.py b/nemo_rl/models/megatron/setup.py
index b0d5be4d9..6f7dca0c0 100644
--- a/nemo_rl/models/megatron/setup.py
+++ b/nemo_rl/models/megatron/setup.py
@@ -98,6 +98,40 @@ from nemo_rl.models.value.config import ValueConfig
 TokenizerType = TypeVar("TokenizerType", bound=PreTrainedTokenizerBase)


+def _apply_target_allowlist_prefixes(config: PolicyConfig) -> None:
+    """Allow extra Megatron config ``_target_`` prefixes requested by policy config."""
+    prefixes = config["megatron_cfg"].get("target_allowlist_prefixes", [])
+    if not prefixes:
+        return
+
+    from megatron.training.config.instantiate_utils import target_allowlist
+
+    for prefix in prefixes:
+        target_allowlist.add_prefix(prefix)
+
+
+class NoRefitMegatronBridge:
+    """Bridge placeholder for colocated Megatron generation paths that do not refit."""
+
+    def __init__(self, transformer_config: Any | None = None):
+        self.transformer_config = transformer_config
+
+    def get_conversion_tasks(self, *_args, **_kwargs):
+        return []
+
+    def export_hf_weights(self, *_args, **_kwargs):
+        raise NotImplementedError(
+            "Colocated Megatron generation reuses the training policy and does not export Hugging Face weights."
+        )
+
+
+def _uses_colocated_megatron_generation(config: PolicyConfig) -> bool:
+    """Return whether policy generation is Megatron and colocated with training."""
+    generation = config.get("generation") or {}
+    colocated = (generation.get("colocated") or {}).get("enabled", True)
+    return generation.get("backend") == "megatron" and colocated is not False
+
+
 def destroy_parallel_state():
     """Safely destroy parallel state and reset async call tracking.

@@ -464,6 +498,7 @@ def setup_model_config(
                 "directory not mounted on this node. Please check."
             )

+        _apply_target_allowlist_prefixes(config)
         try:
             cfg_from_pretrained = ConfigContainer.from_yaml(
                 pretrained_run_config, mode=InstantiationMode.STRICT
@@ -950,6 +985,8 @@ def _create_megatron_config(
         "overlap_param_gather": overlap_param_gather,
         "reuse_grad_buf_for_mxfp8_param_ag": reuse_grad_buf_for_mxfp8_param_ag,
     }
+    tokenizer_cfg = config.get("tokenizer") or {}
+    tokenizer_model = tokenizer_cfg.get("name") or hf_model_name

     return ConfigContainer(
         model=model_cfg,
@@ -986,7 +1023,7 @@ def _create_megatron_config(
         dataset=None,
         tokenizer=TokenizerConfig(
             tokenizer_type="HuggingFaceTokenizer",
-            tokenizer_model=hf_model_name,
+            tokenizer_model=tokenizer_model,
         ),
     )

@@ -1554,9 +1591,11 @@ def finalize_megatron_setup(
         pg_collection=ProcessGroupCollection.use_mpu_process_groups(),
     )

+    tokenizer_cfg = config.get("tokenizer") or {}
+    tokenizer_model = tokenizer_cfg.get("name") or hf_model_name
     tokenizer_config = TokenizerConfig(
         tokenizer_type="HuggingFaceTokenizer",
-        tokenizer_model=hf_model_name,
+        tokenizer_model=tokenizer_model,
         hf_tokenizer_kwargs={
             "trust_remote_code": True,
             "use_fast": True,
@@ -1571,9 +1610,10 @@ def finalize_megatron_setup(
     )

     dp_size = worker_sharding_annotations.get_axis_size("data_parallel")
-    megatron_bridge = AutoBridge.from_hf_pretrained(
-        hf_model_name, trust_remote_code=True
-    )
+    if _uses_colocated_megatron_generation(config):
+        megatron_bridge = NoRefitMegatronBridge(transformer_config=getattr(model, "config", None))
+    else:
+        megatron_bridge = AutoBridge.from_hf_pretrained(hf_model_name, trust_remote_code=True)

     should_disable_forward_pre_hook = (
         config["megatron_cfg"]["optimizer"]["use_distributed_optimizer"]
diff --git a/nemo_rl/models/policy/lm_policy.py b/nemo_rl/models/policy/lm_policy.py
index f1a5d705c..0b86a145b 100644
--- a/nemo_rl/models/policy/lm_policy.py
+++ b/nemo_rl/models/policy/lm_policy.py
@@ -303,7 +303,7 @@ class Policy(ColocatablePolicyInterface, GenerationInterface):
             self.flops_tracker = FLOPTracker.from_config(
                 config["model_name"], get_default_hf_config(config["model_name"])
             )
-        except ValueError as e:
+        except (OSError, ValueError) as e:
             self.flops_tracker = None
             print(f"FLOPS tracker not supported for model {config['model_name']}: {e}")

diff --git a/nemo_rl/models/policy/workers/megatron_policy_worker.py b/nemo_rl/models/policy/workers/megatron_policy_worker.py
index e517e0298..e40a11294 100644
--- a/nemo_rl/models/policy/workers/megatron_policy_worker.py
+++ b/nemo_rl/models/policy/workers/megatron_policy_worker.py
@@ -1167,6 +1167,13 @@ class MegatronPolicyWorkerImpl(
     @wrap_with_nvtx_name("megatron_policy_worker/prepare_refit_info")
     def prepare_refit_info(self) -> None:
         """Prepare state dict metadata for weight refitting and IPC streaming."""
+        generation = self.cfg.get("generation") or {}
+        colocated = (generation.get("colocated") or {}).get("enabled", True)
+        if generation.get("backend") == "megatron" and colocated is not False:
+            self.refit_param_info_mcore = []
+            self.refit_conversion_tasks = []
+            return None
+
         self.refit_param_info_mcore = self._calculate_refit_param_info()

         # Collect tensor metadata for refit / hf side info
@@ -1382,6 +1389,7 @@ class MegatronPolicyWorkerImpl(
             and self.optimizer is not None
             and not self.optimizer_cpu_offload
             and self.offload_optimizer_for_logprob
+            and not self._generation_adapter_preserves_optimizer_state()
         ):
             self.move_optimizer("cpu")

@@ -1404,6 +1412,8 @@ class MegatronPolicyWorkerImpl(
         ):
             self.move_optimizer("cuda")

+        self._generation_offload_before_refit_complete = False
+
         if self.cfg["megatron_cfg"]["empty_unused_memory_level"] >= 1:
             torch.cuda.empty_cache()

@@ -1518,9 +1528,12 @@ class MegatronPolicyWorkerImpl(
             hasattr(self, "optimizer")
             and self.optimizer is not None
             and not self.optimizer_cpu_offload
+            and not self._generation_adapter_preserves_optimizer_state()
         ):
             self.move_optimizer("cpu")

+        self._generation_offload_before_refit_complete = True
+
         gc.collect()
         torch.cuda.empty_cache()

@@ -1537,10 +1550,23 @@ class MegatronPolicyWorkerImpl(
         """Offload as much as possible on the CPU."""
         no_grad = torch.no_grad()
         no_grad.__enter__()
-        self.model = self.move_model(self.model, "cpu")
+        preserve_model_storage = (
+            self._generation_adapter_requires_persistent_model_storage()
+        )
+        prior_offload_complete = getattr(
+            self, "_generation_offload_before_refit_complete", False
+        )
+        self.model = self.move_model(
+            self.model,
+            "cpu",
+            move_params=not preserve_model_storage,
+            move_grads=False,
+        )
         self.model.eval()
         torch.randn(1).cuda()  # wake up torch allocator
-        self.offload_before_refit()  # rerun the old offload function
+        if not prior_offload_complete:
+            self.offload_before_refit()
+        self._generation_adapter_model_refit_complete()

         allocated = torch.cuda.memory_allocated() / (1024**3)  # Convert to GB
         reserved = torch.cuda.memory_reserved() / (1024**3)  # Convert to GB
diff --git a/nemo_rl/distributed/virtual_cluster.py b/nemo_rl/distributed/virtual_cluster.py
index 7d50355..ea55326 100644
--- a/nemo_rl/distributed/virtual_cluster.py
+++ b/nemo_rl/distributed/virtual_cluster.py
@@ -183,7 +183,12 @@ def _get_free_port_local(
     return port


-def init_ray(log_dir: Optional[str] = None) -> None:
+def init_ray(
+    log_dir: Optional[str] = None,
+    *,
+    include_dashboard: bool = True,
+    num_cpus: Optional[int] = None,
+) -> None:
     """Initialise Ray.

     Try to attach to an existing local cluster.
@@ -192,6 +197,8 @@ def init_ray(log_dir: Optional[str] = None) -> None:

     Args:
         log_dir: Optional directory to store Ray logs and temp files.
+        include_dashboard: Whether a newly created local cluster starts the dashboard.
+        num_cpus: Optional CPU limit for a newly created local cluster.
     """
     # Set up runtime environment
     env_vars = dict(os.environ)
@@ -260,12 +267,16 @@ def init_ray(log_dir: Optional[str] = None) -> None:
     local_runtime_env = dict(runtime_env)
     local_runtime_env.pop("working_dir", None)

+    local_init_kwargs = {}
+    if num_cpus is not None:
+        local_init_kwargs["num_cpus"] = num_cpus
     ray.init(
         log_to_driver=True,
-        include_dashboard=True,
+        include_dashboard=include_dashboard,
         runtime_env=local_runtime_env,
         _temp_dir=os.path.abspath(log_dir) if log_dir else None,
         resources={cvd_tag: 1},
+        **local_init_kwargs,
     )
     logger.info(
         f"Started local cluster with tag '{cvd_tag}': {ray.cluster_resources()}"
diff --git a/nemo_rl/algorithms/logits_sampling_utils.py b/nemo_rl/algorithms/logits_sampling_utils.py
index d90451593..8bac86a45 100644
--- a/nemo_rl/algorithms/logits_sampling_utils.py
+++ b/nemo_rl/algorithms/logits_sampling_utils.py
@@ -70,6 +70,7 @@ def need_top_k_or_top_p_filtering(
 def _apply_top_k_only_fn(
     logits: torch.Tensor,
     top_k: int | None,
+    target_token_ids: torch.Tensor | None = None,
 ) -> tuple[torch.Tensor, torch.Tensor | None]:
     """Apply top-k mask to the logits.

@@ -84,6 +85,7 @@ def _apply_top_k_only_fn(
     Args:
         logits: Input logits tensor of shape [*, vocab_size].
         top_k: Top-k sampling parameter.
+        target_token_ids: Optional observed tokens to union into support during policy replay.

     Returns:
         filtered_logits: Filtered logits tensor with the same shape as input logits.
@@ -98,6 +100,8 @@ def _apply_top_k_only_fn(
     top_k_values, _ = torch.topk(logits, top_k, dim=-1)
     threshold = top_k_values[..., -1:].expand_as(logits)
     keep_mask = logits >= threshold
+    if target_token_ids is not None:
+        keep_mask.scatter_(-1, target_token_ids.unsqueeze(-1), True)

     # Apply mask: keep top-k values, set others to -inf
     logits = torch.where(
@@ -114,6 +118,7 @@ def _apply_top_k_top_p_fn(
     top_k: int | None,
     top_p: float,
     chunk_size: int | None = TOP_K_TOP_P_CHUNK_SIZE,
+    target_token_ids: torch.Tensor | None = None,
 ) -> tuple[torch.Tensor, torch.Tensor | None]:
     """Apply top-k and top-p masks to the logits with chunking for memory efficiency.

@@ -134,22 +139,34 @@ def _apply_top_k_top_p_fn(
         top_k: Top-k sampling parameter. Set to -1 or None to consider all tokens.
         top_p: Top-p (nucleus) sampling parameter. Must be in (0, 1]. Set to 1 to consider all tokens
         chunk_size: Number of tokens to process per chunk for memory efficiency. Defaults to TOP_K_TOP_P_CHUNK_SIZE.
+        target_token_ids: Optional observed tokens to union into support during policy replay.

     Returns:
         filtered_logits: Filtered logits tensor with the same shape as input logits.
         keep_mask: Mask tensor with the same shape as input logits, where 1 (True) indicates
             tokens to be kept, 0 (False) indicates tokens to be masked.
     """
+    original_shape = logits.shape
+    vocab_size = logits.shape[-1]
+    if target_token_ids is not None:
+        if target_token_ids.shape != original_shape[:-1]:
+            raise ValueError(
+                f"target_token_ids shape {target_token_ids.shape} must match logits prefix shape {original_shape[:-1]}"
+            )
+        target_token_ids = target_token_ids.to(device=logits.device, dtype=torch.long)
+
     if not _need_top_p_filtering(top_p):
         if not _need_top_k_filtering(top_k):
             return logits, None
         # Avoid sorting vocab for top-k only case
-        filtered_logits, top_k_keep_mask = _apply_top_k_only_fn(logits, top_k)
+        filtered_logits, top_k_keep_mask = _apply_top_k_only_fn(
+            logits, top_k, target_token_ids
+        )
         return filtered_logits, top_k_keep_mask

     # Save original shape and flatten to 2D for consistent chunking
-    original_shape = logits.shape
-    vocab_size = logits.shape[-1]
+    if target_token_ids is not None:
+        target_token_ids = target_token_ids.reshape(-1)
     logits = logits.view(-1, vocab_size)  # [*, vocab_size] -> [num_tokens, vocab_size]
     num_tokens = logits.shape[0]

@@ -198,6 +215,11 @@ def _apply_top_k_top_p_fn(
         else:
             chunk_mask = top_p_keep_mask_chunk
         chunk_mask = chunk_mask.scatter(dim=-1, index=logits_idx, src=chunk_mask)
+        if target_token_ids is not None:
+            chunk_targets = target_token_ids[start_idx:end_idx].unsqueeze(-1)
+            target_logits = chunk_logits.gather(-1, chunk_targets)
+            chunk_filtered.scatter_(-1, chunk_targets, target_logits)
+            chunk_mask.scatter_(-1, chunk_targets, True)

         # Store results
         filtered_logits[start_idx:end_idx, :] = chunk_filtered
@@ -220,6 +242,7 @@ class _ApplyTopKTopP(torch.autograd.Function):
         top_k: Optional[int],
         top_p: float,
         chunk_size: int | None = TOP_K_TOP_P_CHUNK_SIZE,
+        target_token_ids: torch.Tensor | None = None,
     ) -> tuple[torch.Tensor, torch.Tensor | None]:
         """Apply top-k/top-p filtering and save masks for backward.

@@ -228,9 +251,10 @@ class _ApplyTopKTopP(torch.autograd.Function):
             top_k: Top-k sampling parameter. Set to -1 or None to consider all tokens.
             top_p: Top-p sampling parameter. Must be in (0, 1]. Set to 1 to consider all tokens.
             chunk_size: Number of tokens to process per chunk. Defaults to TOP_K_TOP_P_CHUNK_SIZE.
+            target_token_ids: Optional observed tokens to union into support during policy replay.
         """
         filtered_logits, keep_mask = _apply_top_k_top_p_fn(
-            logits, top_k, top_p, chunk_size
+            logits, top_k, top_p, chunk_size, target_token_ids
         )

         # Save masks for backward pass
@@ -248,7 +272,7 @@ class _ApplyTopKTopP(torch.autograd.Function):
         if keep_mask is not None:
             grad_filtered_logits = grad_filtered_logits.masked_fill(~keep_mask, 0.0)

-        return grad_filtered_logits, None, None, None
+        return grad_filtered_logits, None, None, None, None


 def apply_top_k_top_p(
@@ -256,6 +280,7 @@ def apply_top_k_top_p(
     top_k: int | None,
     top_p: float,
     chunk_size: int | None = TOP_K_TOP_P_CHUNK_SIZE,
+    target_token_ids: torch.Tensor | None = None,
 ) -> tuple[torch.Tensor, torch.Tensor | None]:
     """Apply top-k and top-p masks to the logits with proper gradient handling.

@@ -274,6 +299,9 @@ def apply_top_k_top_p(
         top_k: Top-k sampling parameter. Set to -1 to consider all tokens.
         top_p: Top-p (nucleus) sampling parameter. Must be in (0, 1]. Set to 1 to consider all tokens.
         chunk_size: Number of tokens to process per chunk. Defaults to TOP_K_TOP_P_CHUNK_SIZE.
+        target_token_ids: Optional observed tokens to union into support. This is used when replaying
+            sampled actions against recomputed logits so a top-k/top-p boundary change cannot turn
+            a valid action log-probability into negative infinity.

     Returns:
         filtered_logits: Filtered logits tensor with the same shape as input logits.
@@ -282,4 +310,4 @@ def apply_top_k_top_p(
     """
     if not _need_top_k_filtering(top_k) and not _need_top_p_filtering(top_p):
         return logits, None
-    return _ApplyTopKTopP.apply(logits, top_k, top_p, chunk_size)
+    return _ApplyTopKTopP.apply(logits, top_k, top_p, chunk_size, target_token_ids)
diff --git a/nemo_rl/distributed/model_utils.py b/nemo_rl/distributed/model_utils.py
index ad253cf3e..3e7a8ab24 100644
--- a/nemo_rl/distributed/model_utils.py
+++ b/nemo_rl/distributed/model_utils.py
@@ -432,7 +432,10 @@ class DistributedLogprobWithSampling(torch.autograd.Function):

         # Apply top-k and top-p filtering locally (returns keep_mask for gradient)
         logits, keep_mask = apply_top_k_top_p(
-            seq_parallel_logits, top_k=top_k, top_p=top_p
+            seq_parallel_logits,
+            top_k=top_k,
+            top_p=top_p,
+            target_token_ids=target_local,
         )

         # Compute log softmax
@@ -589,7 +592,10 @@ class ChunkedDistributedLogprobWithSampling(torch.autograd.Function):

             # Apply top-k and top-p filtering locally
             logits_chunk, _ = apply_top_k_top_p(
-                seq_parallel_logits_chunk, top_k=top_k, top_p=top_p
+                seq_parallel_logits_chunk,
+                top_k=top_k,
+                top_p=top_p,
+                target_token_ids=target_local,
             )

             # Compute log softmax
@@ -678,7 +684,10 @@ class ChunkedDistributedLogprobWithSampling(torch.autograd.Function):

             # Rematerialize filtering
             logits_chunk, keep_mask = apply_top_k_top_p(
-                seq_parallel_logits_chunk, top_k=top_k, top_p=top_p
+                seq_parallel_logits_chunk,
+                top_k=top_k,
+                top_p=top_p,
+                target_token_ids=target_local,
             )

             # Rematerialize softmax
@@ -1624,17 +1633,18 @@ def get_next_token_logprobs_from_logits(
     else:
         # Remove last position's logits
         next_token_logits_wo_last = next_token_logits[:, :-1]
+        next_tokens = input_ids[:, 1:].cuda()  # Skip first token
         # Apply top-k and top-p filtering
         next_token_logits_wo_last, _ = apply_top_k_top_p(
             next_token_logits_wo_last,
             top_k=sampling_params.top_k if sampling_params is not None else None,
             top_p=sampling_params.top_p if sampling_params is not None else 1.0,
+            target_token_ids=next_tokens,
         )
         # Compute logprobs
         next_token_logprobs = torch.nn.functional.log_softmax(
             next_token_logits_wo_last, dim=-1
         )
-        next_tokens = input_ids[:, 1:].cuda()  # Skip first token
         logprobs = next_token_logprobs.gather(
             dim=-1, index=next_tokens.unsqueeze(-1)
         ).squeeze(-1)
